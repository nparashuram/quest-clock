<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.Clock"
        tools:targetApi="31">

        <activity
            android:name=".DigitalClockActivity"
            android:exported="true"
            android:taskAffinity="com.nparashuram.quest.clock.digital"
            android:theme="@style/Theme.Clock.Transparent">
            <layout
                android:defaultWidth="640dp"
                android:defaultHeight="640dp" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".StopwatchActivity"
            android:exported="false"
            android:taskAffinity="com.nparashuram.quest.clock.stopwatch"
            android:theme="@style/Theme.Clock.Transparent">
            <layout
                android:defaultWidth="640dp"
                android:defaultHeight="640dp" />
        </activity>

        <activity
            android:name=".TimerActivity"
            android:exported="false"
            android:taskAffinity="com.nparashuram.quest.clock.timer"
            android:theme="@style/Theme.Clock.Transparent">
            <layout
                android:defaultWidth="640dp"
                android:defaultHeight="640dp" />
        </activity>
        <activity
            android:name=".AlarmActivity"
            android:exported="true"
            android:taskAffinity="com.nparashuram.quest.clock.alarm"
            android:theme="@style/Theme.Clock.Transparent">
            <layout
                android:defaultWidth="640dp"
                android:defaultHeight="640dp" />
        </activity>

        <receiver
            android:name=".AlarmReceiver"
            android:exported="true" />

    </application>

</manifest> 